# Changesets — MoTDex

Track user-visible changes for MoTDex releases. MoTDex is currently a private static site; this folder is ready for when tagged releases begin.

Run `pnpm changeset` (after `@changesets/cli` is installed) to add a changeset file. See the `changesets` skill in `.agents/skills/changesets/SKILL.md`.
